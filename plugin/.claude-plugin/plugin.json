{
  "name": "dispatch-guards",
  "version": "0.1.12",
  "description": "Mechanical guards for disciplined subagent dispatching: model gate (deny/ask tiers, escalation lane, title+name prefixes), subagent push gate, push-claim reminder, brief/report reminders, closing-report enforcer, dispatch log. Mechanism ships here; site policy lives in ~/.claude/dispatch-guards.json."
}
