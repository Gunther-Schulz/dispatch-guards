{
  "name": "dispatch-guards",
  "version": "0.4.0",
  "description": "The dispatch discipline as a skill plus its mechanical guards. Skills: dispatch (decision-complete briefs, closing-report + roadmap forms, dispatcher duties, tier-readiness register, Codex routing — the operational corpus' dispatch half) and worktree (portable git-worktree/git-hook mechanics). Guards: dispatch-skill load gate, model gate (deny/ask tiers, escalation lane, title+name prefixes), subagent push gate, push-claim reminder, amend gate, brief/report reminders, closing-report enforcer, message-payload gate, discovery-volume reminder, dispatch log. Mechanism ships here; site policy lives in ~/.claude/dispatch-guards.json."
}
